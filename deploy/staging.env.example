# Example staging environment variables
# Use this file as a template when flipping the SSH WebProfile delegation on in staging.
# Set AUTH_SSH_USE_WEBPROFILE_API=true to make web call the Go webprofile shim for SSH operations.

AUTH_SSH_USE_WEBPROFILE_API=true
# Other staging-specific env vars can go here (MONGO_URL, REDIS_HOST, etc.)
