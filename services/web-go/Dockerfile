# Dockerfile template for web-go

FROM golang:1.25-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /bin/web-go

FROM scratch
COPY --from=build /bin/web-go /bin/web-go
ENTRYPOINT ["/bin/web-go"]
