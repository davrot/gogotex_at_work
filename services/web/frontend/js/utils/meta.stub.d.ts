declare module '@/utils/meta' {
  // Minimal stub for focused type-checks of components that import fetch-json
  // Real implementation is complex and pulls in many modules; this stub keeps checks narrow.
  export default function getMeta(name: string): any
}
