This directory contains simple scripts to compare introspection latency between Node and Go webprofile implementations.

Usage:
./run_benchmarks.sh <node_base_url> <go_base_url> <out_dir>

Produces JSON files with basic latency stats for each backend and a comparative summary.
