# Example repository toggle for parity strictness
# Create `ci/PARITY_STRICT` with the single line `true` to enable strict parity checks (blocking).
# To disable strict checks remove the file or change its content to `false`.
true
