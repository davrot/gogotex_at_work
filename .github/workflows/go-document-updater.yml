name: "CI: Go Document-Updater Service"

on:
  push:
    paths:
      - "services/document-updater/**"
  pull_request:
    paths:
      - "services/document-updater/**"

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.20"
      - name: Run go vet
        run: |
          cd services/document-updater
          go vet ./...
      - name: Run tests
        run: |
          cd services/document-updater
          go test ./...
