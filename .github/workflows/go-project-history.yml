name: "CI: Go Project-History Service"

on:
  push:
    paths:
      - "services/project-history/**"
  pull_request:
    paths:
      - "services/project-history/**"

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.20"
      - name: Run go vet
        run: |
          cd services/project-history
          go vet ./...
      - name: Run tests
        run: |
          cd services/project-history
          go test ./...
