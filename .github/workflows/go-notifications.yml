name: "CI: Go Notifications Service"

on:
  push:
    paths:
      - "services/notifications/**"
  pull_request:
    paths:
      - "services/notifications/**"

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.20"
      - name: Run go vet
        run: |
          cd services/notifications
          go vet ./...
      - name: Run tests
        run: |
          cd services/notifications
          go test ./...
