name: "CI: Go Real-time Service"

on:
  push:
    paths:
      - "services/real-time/**"
  pull_request:
    paths:
      - "services/real-time/**"

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.20"
      - name: Run go vet
        run: |
          cd services/real-time
          go vet ./...
      - name: Run tests
        run: |
          cd services/real-time
          go test ./...
